import React from 'react';

import 'styles/Header.scss';

const Header = () => (
  <div className="header">
    <h1>ReactTemplate</h1>
  </div>
);

const Sum = (a, b) => a + b;

export default Header;

export { Sum };
